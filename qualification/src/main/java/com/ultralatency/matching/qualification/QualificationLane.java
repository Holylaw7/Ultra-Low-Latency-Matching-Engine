package com.ultralatency.matching.qualification;

/** Explicit qualification execution lanes. */
public enum QualificationLane {
    /** The approved full evidence lane. */
    FULL,
    /** Short deterministic lane used only by unit tests. */
    TEST
}
