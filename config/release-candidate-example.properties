# Strict UTF-8 properties-v1 example for the local release-candidate runtime.
storage.wal.directory=./data/wal
storage.snapshot.directory=./data/snapshot
recovery.mode=PURE_WAL
wal.durability.mode=SYNC_EACH_APPEND
pipeline.wait.mode=BLOCKING
protocol.bind.address=127.0.0.1
protocol.port=9000
management.enabled=true
management.bind.address=127.0.0.1
management.port=9001
